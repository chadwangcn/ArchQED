# ArchQED
Architecture + Q.E.D.让架构可执行，让完成有证据(Compile architecture. Prove implementation),无证据，不完成(Compile architecture. Prove implementation)。

ArchQED is an open protocol and skill suite that compiles human-readable architecture into machine contracts and accepts only evidence-backed completion.
